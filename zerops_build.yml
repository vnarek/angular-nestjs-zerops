# select what technology your build uses
use: [ nodejs@13, go@1 ]
# which commands to run to build
run:
  - npm i
  - npm run bui:production
# which files / folders to deploy after build
deploy: [ dist, node_modules, package.json ]
